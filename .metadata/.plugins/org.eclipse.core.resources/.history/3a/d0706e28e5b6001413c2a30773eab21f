package JUnitTest;
import java.io.File;

import ProjectCode.ValueStoreException;
import ProjectCode.ValueStoreImpl;

import org.junit.Before;
import org.junit.Test;

import junit.framework.TestCase;


public class ValueStoreTest extends TestCase {
	@Before
	public void initialize(){
		ValueStoreImpl valueStore = new ValueStoreImpl();
	}

	@Test
	public void testPut() {
		
		File entry = new File(valueStore.getWorkingDirectory + key + ".txt");
		
		if(entry.exists()){
			entry.delete();
			System.out.println("Entry Deleted");
		}
		else{
			throw new ValueStoreException("No value exists at key: " + key);
		}
	}

}
